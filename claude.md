# CLAUDE.md

흔한 LLM 코딩 실수를 줄이기 위한 행동 지침. 프로젝트별 지침과 병합해서 사용.

**트레이드오프:** 이 지침은 속도보다 신중함을 우선시합니다. 단순한 작업에는 판단해서 적용하세요.

## 1. 코딩 전에 생각하기

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:
- 가정한 내용을 명시적으로 밝힌다. 불확실하면 질문한다.
- 해석이 여러 가지라면 제시한다 — 조용히 하나를 고르지 않는다.
- 더 단순한 방법이 있다면 말한다. 필요할 때는 반론을 제기한다.
- 불명확한 것이 있으면 멈춘다. 무엇이 혼란스러운지 명시하고 질문한다.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드만. 추측성 코드는 금지.**

- 요청받지 않은 기능은 추가하지 않는다.
- 일회성 코드에 추상화를 넣지 않는다.
- 요청받지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 불가능한 시나리오에 대한 에러 처리를 하지 않는다.
- 200줄로 썼는데 50줄로 줄일 수 있다면, 다시 작성한다.

스스로에게 물어볼 것: "시니어 엔지니어가 이걸 보고 과하다고 할까?" 그렇다면 단순화한다.

## 3. 외과적 수정

**반드시 필요한 것만 건드린다. 내가 만든 것만 정리한다.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩토링하지 않는다.
- 내 방식이 달라도 기존 스타일을 맞춘다.
- 관련 없는 데드 코드를 발견하면, 언급은 하되 삭제하지 않는다.

내 변경으로 고아가 생긴 경우:
- 내 변경으로 인해 사용되지 않게 된 import/변수/함수는 제거한다.
- 원래부터 있던 데드 코드는 요청받지 않는 한 제거하지 않는다.

기준: 변경된 모든 줄은 사용자의 요청에서 직접 이어져야 한다.

## 4. 목표 중심 실행

**성공 기준을 정의한다. 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 변환:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시킨다"
- "X 리팩토링" → "전후로 테스트가 통과하는지 확인한다"

여러 단계의 작업에는 간략한 계획을 먼저 제시: