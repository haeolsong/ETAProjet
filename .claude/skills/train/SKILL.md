---
name: train
description: 김해공항 ETA 예측 모델의 학습·평가 절차. 베이스라인/사전예측/이륙후 3종 모델 비교, 시계열 분할 규칙, MAE·RMSE 보고 방식을 다룬다. 모델을 학습하거나 성능을 평가할 때 사용한다.
---

# 모델 학습·평가 절차

> ⚠️ `src/train.py` 는 동작하지만, 데이터가 2026-09-09 부터 하루씩만 쌓인다.
> 표본이 모이기 전의 성능 수치는 파이프라인 검증용이지 결과가 아니다.

## 타겟 변수 (확정)

```
delay_minutes = estimateddatetime - scheduledatetime   # 분
```

`data/raw/flights_*.csv` 의 `io == "I"`(도착) 행이 대상이다.

전처리는 `src/preprocess.py` 가 하고, `train.py` 는 `dataset.parquet` 을 읽기만 한다.
전처리에서 거르는 것(직접 다시 구현하지 말 것):

1. **코드쉐어 제거** — `masterflightid` 는 **코드쉐어 행에만** 채워진다.
   `flightid == masterflightid` 만 남기면 단독편이 전부 날아간다.
   조건은 **결측이거나 주편명**. 그 뒤 `fid` 중복 제거가 한 번 더 필요하다.
2. **미확정편 제거** — `rmkKor == '도착'` 만 남긴다. 결항편은
   `estimateddatetime == scheduledatetime` 이라 **지연 0분처럼 보이고**,
   미도착편의 시각은 실적이 아니라 예정값이다.
3. 실측 참고(2026-09-07~10 도착 518편): 평균 −5.2분, 중앙값 −9분, 표준편차 22.2분.
   **조기도착이 흔하다** — 베이스라인 중앙값이 음수가 된다.

```bash
source venv/bin/activate
python src/preprocess.py
python src/train.py                    # 테스트셋 뒤쪽 20%
python src/train.py --test-frac 0.3
```

## 반드시 지킬 규칙

### 1. 랜덤 분할 금지

시계열 데이터다. `train_test_split(shuffle=True)`를 쓰면 미래 정보가 학습셋에 섞여
성능이 부풀려진다. **시간 기준으로 자른다** — 예: 마지막 3주를 테스트셋.

### 2. 베이스라인을 반드시 함께 보고

지연 분포는 0분 근처에 몰려 있고 가끔 크게 터진다. 그래서 **"항상 학습셋 중앙값을
예측하는" 모델도 MAE 12~15분**이 나온다.

목표인 "MAE ≤ 15분"은 이 바보 모델로도 달성될 수 있다.
베이스라인 비교표가 없으면 성능 수치가 아무 의미를 갖지 못한다.

### 3. 세 모델을 같은 코드로 비교

차이는 피처 목록 한 줄뿐이다.

| 모델 | 피처 | 역할 |
|------|------|------|
| 베이스라인 | 없음 (중앙값 고정) | 기준선 |
| 사전 예측 | `FEATURES_PRE` — 기상·노선·시간대 | **메인 서사.** 출발 전에 알 수 있는 정보만 |
| 이륙 후 | `FEATURES_PRE + ['dep_delay']` | 성능 상한선 참고 |

`dep_delay`(출발지연)는 도착지연의 압도적 1위 예측 변수다. 넣으면 MAE가 크게 떨어지지만
"출발지연 보고 맞힌 것 아니냐"는 지적을 받는다. 그래서 **메인은 사전 예측**으로 두고
이륙 후 모델은 비교용으로만 쓴다.

**`dep_delay` 는 국내선(GMP·CJU)에만 붙는다** — 전체 도착편의 약 29%. 이 API 가
한국공항공사 소관이라 해외 출발편은 취득 경로가 없다. 인천은 별도 API(B551177)로
가능하지만 정제 후 기여도가 3.3%p 라 붙이지 않았다.

> ⚠️ **표본이 다르면 MAE 를 직접 빼지 말 것.** 사전 예측은 전체, 이륙 후는 29% 에서
> 학습된다. `--dep-delay-only` 로 같은 부분집합에서 세 모델을 비교해야 (X−Y) 가
> 의미를 갖는다.

**이륙 후 모델의 하한선은 약 4~5분이다.** 실측 150편에서 다음이 모든 행에 성립한다.

```
도착지연 − 출발지연 = 실제 비행시간 − 계획 비행시간
```

계획 비행시간은 노선별 고정(60/65분), 실제 비행시간 표준편차는 5.7분. 출발지연을 알면
남는 불확실성이 그 5.7분에 갇힌다. 계획에 약 25분 여유가 있어 **20분 늦게 출발해도
5분 일찍 도착하는 것이 정상**이다. 이륙 후 모델이 MAE 4분대를 찍어도 놀랄 일이 아니며,
그것은 모델의 성능이 아니라 **시간표 구조**를 반영한 값이다.

### 4. 결론 형태

> 출발 전 MAE X분 → 출발지연 정보 추가 시 Y분. 즉 그 정보의 가치는 (X−Y)분.

이 구조면 모델 성능이 낮게 나와도 유효한 분석 결과가 된다.

## 보고 지표

- **MAE** (주지표) — 평균적으로 몇 분 틀렸나
- **RMSE** — 큰 오차에 벌점. MAE보다 훨씬 크면 가끔 크게 빗나가는 모델이다
- **R²** — 참고용. 선행연구상 출발 전 예측은 0.10~0.30 수준이 현실적이다

결과는 `data/processed/metrics.csv`에 누적 기록한다. 실험할 때마다 덮어쓰지 말 것.
